name: PR

on:
  pull_request:
    branches:
      - 'main'
    types:
      # update
      # - reopened
      - ready_for_review

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1